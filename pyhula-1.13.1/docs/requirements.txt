# PyHula Dependencies
# Python 3.8+ compatible version

# Core dependencies
numpy>=1.19.0
opencv-python>=4.5.0
pyserial>=3.4
pymavlink>=2.4.0
scipy>=1.6.0
psutil>=5.7.0

# Optional visualization dependencies
matplotlib>=3.3.0
pillow>=8.0.0

# Development dependencies (install with pip install -e .[dev])
pytest>=6.0
pytest-cov>=2.10
black>=21.0
flake8>=3.8

# Documentation generation (for building docs locally)
# Install with: pip install -e .[docs]
sphinx>=3.0.0
sphinx-rtd-theme>=0.5.0

# Build system dependencies
# You'll need these if you're building from source
setuptools>=50.0
wheel>=0.36.0
Cython>=0.29.0  # For development/rebuilding from Cython sources
